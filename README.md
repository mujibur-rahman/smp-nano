# smp-nano

## How to run it

### 1. Clone the repository
```bash
git clone <repository-url>

2. Load extension into Chrome
   1) Open Chrome and go to: chrome://extensions/
   2) Turn Developer mode ON (top-right)
   3) Click Load unpacked
   4) Select your `smp-nano` folder